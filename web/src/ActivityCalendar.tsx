import {useMemo,useState} from "react";
import "./activity-calendar.css";

export type PlannedActivity={id:string;date:string;title:string;description:string;boardColumn?:"todo"|"in_progress"|"review"|"done"};
type ActivityMode="calendar"|"tree"|"kanban";
const iso=(date:Date)=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`;
const monthNames=Array.from({length:12},(_,month)=>new Date(2024,month,1).toLocaleString(undefined,{month:"long"}));

export function activityTitle(markdown:string){
 const line=markdown.split(/\r?\n/).map(value=>value.trim()).find(Boolean)??"Activity";
 const plain=line.replace(/^#{1,6}\s+/,"").replace(/^[-*+]\s+/,"").replace(/^\d+[.)]\s+/,"").replace(/^>\s+/,"").replace(/\[([^\]]+)\]\([^\)]+\)/g,"$1").replace(/[*_~`]/g,"").trim()||"Activity";
 return plain.length>80?`${plain.slice(0,77)}...`:plain;
}

function CalendarIcon(){return <svg aria-hidden="true" viewBox="0 0 20 20"><path d="M5 2v3M15 2v3M3 7h14M4 4h12a1 1 0 0 1 1 1v12H3V5a1 1 0 0 1 1-1Z"/></svg>}
function TreeIcon(){return <svg aria-hidden="true" viewBox="0 0 20 20"><path d="M6 4h8M6 10h8M6 16h8M3 4h.01M3 10h.01M3 16h.01"/></svg>}
function KanbanIcon(){return <svg aria-hidden="true" viewBox="0 0 20 20"><rect x="2.5" y="3" width="4" height="14" rx="1"/><rect x="8" y="3" width="4" height="9" rx="1"/><rect x="13.5" y="3" width="4" height="12" rx="1"/></svg>}

export function ActivityCalendar({activities,onActivitiesChange,finishDate,onFinishDateChange,reviewerCount}:{activities:PlannedActivity[];onActivitiesChange:(items:PlannedActivity[])=>void;finishDate:string;onFinishDateChange:(date:string)=>void;reviewerCount:number}){
 const now=new Date(),today=iso(now),todayYear=now.getFullYear(),todayMonth=now.getMonth();
 const[mode,setMode]=useState<ActivityMode>("calendar");const[current,setCurrent]=useState(()=>new Date(todayYear,todayMonth,1));const[editingDate,setEditingDate]=useState("");const[editingID,setEditingID]=useState("");const[description,setDescription]=useState("");const[editingColumn,setEditingColumn]=useState<PlannedActivity["boardColumn"]>("todo");const[markFinish,setMarkFinish]=useState(false);const[draggedActivityID,setDraggedActivityID]=useState<string|null>(null);const[dropColumn,setDropColumn]=useState<PlannedActivity["boardColumn"]|null>(null);
 const orderedActivities=useMemo(()=>[...activities].sort((a,b)=>a.date.localeCompare(b.date)||a.title.localeCompare(b.title)),[activities]);
 const years=useMemo(()=>Array.from({length:16},(_,index)=>todayYear+index),[todayYear]);
 const days=useMemo(()=>{const first=new Date(current.getFullYear(),current.getMonth(),1);const start=new Date(first);start.setDate(1-first.getDay());return Array.from({length:42},(_,index)=>{const day=new Date(start);day.setDate(start.getDate()+index);return day})},[current]);
 function defaultTreeDate(){if(today!==finishDate)return today;const next=new Date(now);next.setDate(next.getDate()+1);return iso(next)}
 function openNew(date:string,column:PlannedActivity["boardColumn"]="todo"){if(date<today||date===finishDate)return;setEditingDate(date);setEditingID("");setDescription("");setEditingColumn(column);setMarkFinish(false)}
 function openEdit(item:PlannedActivity){setEditingDate(item.date);setEditingID(item.id);setDescription(item.description);setEditingColumn(item.boardColumn??"todo");setMarkFinish(false)}
 function closeEditor(){setEditingDate("");setEditingID("");setDescription("");setEditingColumn("todo");setMarkFinish(false)}
 function save(){
  if(!editingDate||editingDate<today)return;
  if(markFinish){onActivitiesChange(activities.filter(item=>item.date!==editingDate));onFinishDateChange(editingDate);closeEditor();return}
  if(!description.trim()||editingDate===finishDate)return;
  const markdown=description.trim();const next={id:editingID||crypto.randomUUID(),date:editingDate,title:activityTitle(markdown),description:markdown,boardColumn:editingColumn};
  onActivitiesChange(editingID?activities.map(item=>item.id===editingID?next:item):[...activities,next]);closeEditor();
 }
 function moveActivity(column:PlannedActivity["boardColumn"]){if(!draggedActivityID||!column)return;onActivitiesChange(activities.map(item=>item.id===draggedActivityID?{...item,boardColumn:column}:item));setDraggedActivityID(null);setDropColumn(null)}
 function changeYear(year:number){setCurrent(new Date(year,year===todayYear?Math.max(current.getMonth(),todayMonth):current.getMonth(),1))}
 const atCurrentMonth=current.getFullYear()===todayYear&&current.getMonth()===todayMonth;
 const saveDisabled=!editingDate||editingDate<today||(!markFinish&&(!description.trim()||editingDate===finishDate));
 return <div className="activity-calendar">
  <div className="activity-mode-switch" role="tablist" aria-label="Activity planning mode"><button type="button" role="tab" aria-selected={mode==="calendar"} className={mode==="calendar"?"active":""} onClick={()=>setMode("calendar")}><CalendarIcon/>Calendar Mode</button><button type="button" role="tab" aria-selected={mode==="tree"} className={mode==="tree"?"active":""} onClick={()=>setMode("tree")}><TreeIcon/>Tree Mode</button><button type="button" role="tab" aria-selected={mode==="kanban"} className={mode==="kanban"?"active":""} onClick={()=>setMode("kanban")}><KanbanIcon/>Kanban Mode</button></div>
  {mode==="calendar"&&<><div className="calendar-toolbar"><div className="calendar-period"><select aria-label="Calendar month" value={current.getMonth()} onChange={event=>setCurrent(new Date(current.getFullYear(),Number(event.target.value),1))}>{monthNames.map((month,index)=><option key={month} value={index} disabled={current.getFullYear()===todayYear&&index<todayMonth}>{month}</option>)}</select><select aria-label="Calendar year" value={current.getFullYear()} onChange={event=>changeYear(Number(event.target.value))}>{years.map(year=><option key={year} value={year}>{year}</option>)}</select><small>Past dates are read-only. Select + to prefill the activity date.</small></div><div><button type="button" aria-label="Previous month" disabled={atCurrentMonth} onClick={()=>setCurrent(new Date(current.getFullYear(),current.getMonth()-1,1))}>&lt;</button><button type="button" onClick={()=>setCurrent(new Date(todayYear,todayMonth,1))}>Today</button><button type="button" aria-label="Next month" onClick={()=>setCurrent(new Date(current.getFullYear(),current.getMonth()+1,1))}>&gt;</button></div></div>
   <div className="calendar-weekdays">{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(day=><span key={day}>{day}</span>)}</div><div className="calendar-grid">{days.map(day=>{const date=iso(day),items=activities.filter(item=>item.date===date),isFinish=date===finishDate,isPast=date<today;return <div key={date} className={`calendar-day ${day.getMonth()!==current.getMonth()?"outside":""} ${isPast?"past-date":""} ${date===today?"today":""} ${items.length?"has-activity":""} ${isFinish?"finish-date":""}`}><div className="day-head"><time>{day.getDate()}</time>{!isPast&&!isFinish&&<button type="button" aria-label={`Add activity on ${date}`} onClick={()=>openNew(date)}>+</button>}</div>{isFinish&&<span className="finish-label">Finish</span>}{items.length===1&&<button type="button" className="calendar-activity single" onClick={()=>openEdit(items[0])}><strong>{items[0].title}</strong><small>{items[0].description}</small></button>}{items.length>1&&<div className="activity-bubble-wrap"><button type="button" className="activity-count-bubble" aria-label={`${items.length} activities on ${date}`}>{items.length}</button><div className="activity-popover"><strong>{items.length} activities</strong>{items.map(item=><button type="button" key={item.id} onClick={()=>openEdit(item)}><span>{item.title}</span><small>{item.description}</small></button>)}</div></div>}</div>})}</div></>}
  {mode==="tree"&&<section className="activity-tree mode-tree"><div className="activity-tree-head"><div><strong>Activity tree</strong><small>Chronological delivery path</small></div><div className="tree-head-actions"><span>{activities.length} activities</span><button type="button" onClick={()=>openNew(defaultTreeDate())}>Add Activity</button></div></div><div className="tree-track">{orderedActivities.length===0&&!finishDate&&<div className="tree-empty">Add an activity to build the project path.</div>}{orderedActivities.map((item,index)=><button type="button" className="tree-node activity-node" key={item.id} onClick={()=>openEdit(item)}><span className="tree-marker">{index+1}</span><span className="tree-content"><time>{new Date(`${item.date}T12:00:00`).toLocaleDateString(undefined,{day:"numeric",month:"short",year:"numeric"})}</time><strong>{item.title}</strong><small>{item.description}</small></span><span className="tree-edit">Edit</span></button>)}{finishDate&&<><div className="tree-node finish-node"><span className="tree-marker">✓</span><span className="tree-content"><time>{new Date(`${finishDate}T12:00:00`).toLocaleDateString(undefined,{day:"numeric",month:"short",year:"numeric"})}</time><strong>Work finished</strong><small>{reviewerCount?"Project moves to On Review.":"No reviewer configured; project can complete directly."}</small></span></div>{reviewerCount>0&&<div className="tree-node review-node"><span className="tree-marker">R</span><span className="tree-content"><strong>On Review</strong><small>{reviewerCount} reviewer{reviewerCount===1?"":"s"} must approve or request revision.</small></span></div>}<div className="tree-node completed-node"><span className="tree-marker">✓</span><span className="tree-content"><strong>Completed</strong><small>{reviewerCount?"Reached after review approval.":"Reached when work is submitted as finished."}</small></span></div></>}{!finishDate&&<div className="tree-node finish-missing"><span className="tree-marker">!</span><span className="tree-content"><strong>Finish date required</strong><small>Add a date and select Finish before creating this project.</small></span></div>}</div></section>}
  {mode==="kanban"&&<section className="activity-kanban">{([{id:"todo",label:"To do"},{id:"in_progress",label:"In progress"},{id:"review",label:"Review"},{id:"done",label:"Done"}] as const).map(column=>{const items=orderedActivities.filter(item=>(item.boardColumn??"todo")===column.id);return <div className={`kanban-column ${column.id} ${dropColumn===column.id?"drag-over":""}`} onDragEnter={()=>setDropColumn(column.id)} onDragOver={event=>{event.preventDefault();event.dataTransfer.dropEffect="move"}} onDragLeave={event=>{if(!event.currentTarget.contains(event.relatedTarget as Node))setDropColumn(null)}} onDrop={event=>{event.preventDefault();moveActivity(column.id)}} key={column.id}><header><div><i/><strong>{column.label}</strong></div><span>{items.length}</span></header><div className="kanban-cards">{items.map(item=><button type="button" draggable className={`kanban-card ${draggedActivityID===item.id?"dragging":""}`} key={item.id} onDragStart={event=>{setDraggedActivityID(item.id);event.dataTransfer.effectAllowed="move";event.dataTransfer.setData("text/plain",item.id)}} onDragEnd={()=>{setDraggedActivityID(null);setDropColumn(null)}} onClick={()=>openEdit(item)}><time>{new Date(`${item.date}T12:00:00`).toLocaleDateString(undefined,{day:"numeric",month:"short"})}</time><strong>{item.title}</strong><small>{item.description}</small></button>)}{!items.length&&<p>No activities</p>}</div><button type="button" className="kanban-add" onClick={()=>openNew(defaultTreeDate(),column.id)}>+ Add activity</button></div>})}</section>}  {editingDate&&<div className="activity-editor"><div className="editor-head"><div><strong>{editingID?"Edit activity":"Add activity"}</strong><small>Shared activity form · Markdown supported</small></div><button type="button" aria-label="Close activity editor" onClick={closeEditor}>x</button></div><label><span>Date</span><input autoFocus type="date" min={today} value={editingDate} onChange={event=>setEditingDate(event.target.value)} required/></label><label><span>Status</span><select value={editingColumn} disabled={markFinish} onChange={event=>setEditingColumn(event.target.value as PlannedActivity["boardColumn"])}><option value="todo">To do</option><option value="in_progress">In progress</option><option value="review">Review</option><option value="done">Done</option></select></label><label><span>Description</span><textarea disabled={markFinish} rows={7} value={description} onChange={event=>setDescription(event.target.value)} placeholder={"Describe the activity using Markdown\n\n- Deliverable\n- Acceptance criteria"}/><small>Markdown formatting is stored with the activity.</small></label>{!editingID&&<label className="finish-check"><input type="checkbox" checked={markFinish} onChange={event=>setMarkFinish(event.target.checked)}/><span><strong>Finish</strong><small>Use the selected date as the project finish marker instead of an activity.</small></span></label>}{editingDate===finishDate&&!markFinish&&<small className="field-warning">Choose another date; this date is reserved for Finish.</small>}{editingID&&<button type="button" className="delete-activity" onClick={()=>{onActivitiesChange(activities.filter(item=>item.id!==editingID));closeEditor()}}>Delete activity</button>}<div className="editor-actions"><button type="button" onClick={closeEditor}>Cancel</button><button type="button" className="save-activity" disabled={saveDisabled} onClick={save}>{markFinish?"Set finish date":editingID?"Save changes":"Add activity"}</button></div></div>}
 </div>
}
