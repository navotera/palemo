import {useEffect} from "react";
export function ProjectStatusBadge(){useEffect(()=>{const sync=()=>{const header=document.querySelector<HTMLElement>(".execution-head");const context=document.querySelector<HTMLElement>(".detail-context");if(!header||!context)return;const status=context.textContent?.match(/Status:\s*([^·]+)/i)?.[1]?.trim();if(!status)return;let badge=header.querySelector<HTMLElement>(".project-status-badge");if(!badge){badge=document.createElement("span");badge.className="project-status-badge";const edit=header.querySelector(".edit-project-button");(edit?.parentElement??header).appendChild(badge)}badge.textContent=status.replace(/^./,value=>value.toUpperCase())};const observer=new MutationObserver(sync);observer.observe(document.body,{childList:true,subtree:true,characterData:true});sync();return()=>observer.disconnect()},[]);return null}
